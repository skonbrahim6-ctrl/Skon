from pyrogram import Client, filters
from config import BOT_NAME

@Client.on_message(filters.command(["help", "اوامر", "أوامر"]))
async def help_cmds(client, message):
    help_text = f"""
<b>📊 قائمة أوامر بوت {BOT_NAME} (نسخة تيرميكس):</b>

<b>🎵 أوامر التشغيل والتحميل:</b>
• <code>/play</code> + [اسم الأغنية] : للبحث وتحميل الأغنية فوراً.
• <code>/download</code> + [الاسم/الرابط] : لتحميل ملف صوتي MP3.
• <code>/تشغيل</code> : نفس وظيفة أمر play.

<b>🛠 أوامر إضافية:</b>
• <code>/stats</code> : عرض إحصائيات البوت (للمطور فقط).
• <code>/broadcast</code> : إذاعة رسالة (للمطور فقط).

<b>💡 ملاحظة:</b>
تم تحويل نظام المكالمات الصوتية إلى نظام <b>التحميل المباشر</b> لضمان استقرار البوت على الهواتف. يتم حذف الملفات تلقائياً بعد الإرسال للحفاظ على مساحة هاتفك.
"""
    await message.reply_text(help_text)
    
